name: CI

on:
  push:
    branches: [master, security/lts-patches]
  pull_request:
    branches: [master, security/lts-patches]

jobs:
  tests:
    uses: brickhouse-tech/.github/.github/workflows/tests.yml@main
    with:
      node-versions: '["18.x", "20.x", "22.x"]'
      lint: true
